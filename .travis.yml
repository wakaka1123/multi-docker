language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t dyf/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true dyf/react-test npm test

after_success:
  - docker build -t dyf/multi-client ./client
  - docker build -t dyf/multi-nginx ./nginx
  - docker build -t dyf/multi-server ./server
  - docker build -t dyf/multi-worker ./worker
